<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.HiddenActivityForPopup">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="MaterialContainer">
        <item name="android:paddingLeft">@dimen/item_material_left_padding</item>
        <item name="android:paddingStart">@dimen/item_material_left_padding</item>
        <item name="android:paddingRight">@dimen/item_material_left_padding</item>
        <item name="android:paddingEnd">@dimen/item_material_left_padding</item>
        <item name="android:paddingTop">@dimen/item_material_top_padding</item>
        <item name="android:paddingBottom">@dimen/item_material_bottom_padding</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/colorToolbarItems</item>
    </style>

    <style name="ToolbarPopup" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@color/colorToolbarItems</item>
    </style>
</resources>
